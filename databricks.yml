# This is a Databricks asset bundle definition for dab_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_project
  uuid: 358b31f5-97f3-4a3e-bb98-cd3b09aa75c5

include:
  - resources/*.yml
  # - resources/*/*.yml
  
variables:
  catalog:
   default: "citibike_dev"
# resources:
#   jobs:
#     demo_job:
#       name: demo_job
#       tasks:
#         - task_key: notebook_task1
#           notebook_task:
#             notebook_path: notebook/notebook_1.ipynb
#             source: WORKSPACE
#       queue:
#         enabled: true
#       performance_target: STANDARD

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-e380e487-14e8.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-e380e487-14e8.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/ansarivahid547@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/ansarivahid547@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: ansarivahid547@gmail.com
        level: CAN_MANAGE
