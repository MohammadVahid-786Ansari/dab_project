bundle:
  name: dab_project
  uuid: 358b31f5-97f3-4a3e-bb98-cd3b09aa75c5

include:
  - resources/*.yml
  
variables:
  catalog:
    default: "citibike_dev"

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-e380e487-14e8.cloud.databricks.com
    permissions:
      - user_name: ansarivahid547@gmail.com
        level: CAN_MANAGE

  test:
    mode: development   # or production if you want strict deploys
    variables:
      catalog:
        default: "citibike_test"
      dev_flag:
        default: false
    presets:
      name_prefix: '[testing] '    
    workspace:
      host: https://dbc-ba82de44-d338.cloud.databricks.com
      root_path: /Workspace/Users/freelancer548vm@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: freelancer548vm@gmail.com
        level: CAN_MANAGE
